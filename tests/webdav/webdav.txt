*** Settings ***
Resource   lib/stormlib.txt
Resource   lib/clientSRM.txt
Resource   lib/credentials.txt

*** Test Cases ***

Put a file using WebDAV calling the PUT method
  [Setup]  Init certificate and voms proxy  test0  ${vo}
  ${fileName}  Create local file
  ${surl}  Build surl  ${vo}  ${fileName}
  ${options}  Set variable  -X PUT ${fileName}
  ${url}  Build webdav secure URL  ${vo}  ${fileName}
  Execute Curl with proxy and options  ${url}  ${options}
  Perform rm using clientSRM  ${surl}
  Remove local file  ${fileName}
  [Teardown]  Clear all credentials
