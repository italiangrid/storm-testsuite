*** Settings ***

Resource   lib/webdav.txt
Resource   lib/clientSRM.txt
Resource   lib/credentials.txt

*** Test Cases ***

######### ANONYMOUS ##########

WebDAV HEAD root directory as anonymous
  [Tags]  webdav  head  anonymous
  ${url}  Build webdav unsecure root URL
  ${output}  WebDAV anonymous HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK

WebDAV HEAD existent file as anonymous
  [Tags]  webdav  head  anonymous
  ${filename}  Upload file as anonymous
  ${url}  Build webdav unsecure URL  ${ANONYMOUS_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV anonymous HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK

WebDAV HEAD unexistent file as anonymous
  [Tags]  webdav  head  anonymous
  ${filename}  Get a unique name
  ${url}  Build webdav unsecure URL  ${ANONYMOUS_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV anonymous HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  404 Not Found

WebDAV HEAD existent directory as anonymous
  [Tags]  webdav  head  anonymous
  ${url}  Build webdav unsecure URL  ${ANONYMOUS_SA}  ${TESTDIR}
  ${output}  WebDAV anonymous HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK

WebDAV HEAD unexistent directory as anonymous
  [Tags]  webdav  head  anonymous
  ${url}  Build webdav unsecure URL  ${ANONYMOUS_SA}  ${TESTDIR}/fakedirectory
  ${output}  WebDAV anonymous HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  404 Not Found

WebDAV HEAD resource with missing parent as anonymous
  [Tags]  webdav  head  anonymous
  ${url}  Build webdav unsecure URL  ${ANONYMOUS_SA}  ${TESTDIR}/fakedirectory/fakefile.txt
  ${output}  WebDAV anonymous HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  404 Not Found

WebDAV HEAD busy file as anonymous
  [Tags]  webdav  head  anonymous
  ${filename}  Get a unique name
  ${surl}  Build surl  ${ANONYMOUS_SA}  ${TESTDIR}/${filename}
  Init certificate and voms proxy  test0  ${vo}
  ${output}  ${token}  Perform ptp using clientSRM  ${surl}  -p
  Should Contain  ${output}  SRM_SPACE_AVAILABLE
  ${url}  Build webdav unsecure URL  ${ANONYMOUS_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV anonymous HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK
  ${output}  Perform abort file using clientSRM  ${surl}  ${token}
  Should Contain  ${output}  SRM_SUCCESS
  Clear all credentials

WebDAV HEAD existent file as anonymous on unreadable storage area
  [Tags]  webdav  head  anonymous
  ${filename}  Upload file with voms proxy
  ${url}  Build webdav unsecure URL  ${VOMSPROXY_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV anonymous HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  403

####### VOMS PROXY #########

WebDAV HEAD root directory with VOMS proxy
  [Tags]  webdav  head  vomsproxy
  ${url}  Build webdav secure root URL
  ${output}  WebDAV voms proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK

WebDAV HEAD existent file with VOMS proxy
  [Tags]  webdav  head  vomsproxy
  ${filename}  Upload file with voms proxy
  ${url}  Build webdav secure URL  ${VOMSPROXY_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV voms proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK

WebDAV HEAD unexistent file with VOMS proxy
  [Tags]  webdav  head  vomsproxy
  ${filename}  Get a unique name
  ${url}  Build webdav secure URL  ${VOMSPROXY_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV voms proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  404 Not Found

WebDAV HEAD existent directory with VOMS proxy
  [Tags]  webdav  head  vomsproxy
  ${url}  Build webdav secure URL  ${VOMSPROXY_SA}  ${TESTDIR}
  ${output}  WebDAV voms proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK

WebDAV HEAD unexistent directory with VOMS proxy
  [Tags]  webdav  head  vomsproxy
  ${url}  Build webdav secure URL  ${VOMSPROXY_SA}  ${TESTDIR}/fakedirectory
  ${output}  WebDAV voms proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  404 Not Found

WebDAV HEAD resource with missing parent with VOMS proxy
  [Tags]  webdav  head  vomsproxy
  ${url}  Build webdav secure URL  ${VOMSPROXY_SA}  ${TESTDIR}/fakedirectory/fakefile.txt
  ${output}  WebDAV voms proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  404 Not Found

WebDAV HEAD busy file with VOMS proxy
  [Tags]  webdav  head  vomsproxy
  ${filename}  Get a unique name
  ${surl}  Build surl  ${VOMSPROXY_SA}  ${TESTDIR}/${filename}
  Init certificate and voms proxy  test0  ${vo}
  ${output}  ${token}  Perform ptp using clientSRM  ${surl}  -p
  Should Contain  ${output}  SRM_SPACE_AVAILABLE
  ${url}  Build webdav secure URL  ${VOMSPROXY_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV voms proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK
  ${output}  Perform abort file using clientSRM  ${surl}  ${token}
  Should Contain  ${output}  SRM_SUCCESS
  Clear all credentials

WebDAV HEAD existent file with VOMS proxy on an anonymous readable storage area
  [Tags]  webdav  head  vomsproxy
  ${filename}  Upload file as anonymous
  ${url}  Build webdav secure URL  ${ANONYMOUS_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV voms proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK

######### PLAIN PROXY ##########

WebDAV HEAD root directory with plain proxy
  [Tags]  webdav  head  plainproxy
  ${url}  Build webdav secure root URL
  ${output}  WebDAV plain proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK

WebDAV HEAD existent file with plain proxy
  [Tags]  webdav  head  plainproxy
  ${filename}  Upload file with plain proxy
  ${url}  Build webdav secure URL  ${PLAINPROXY_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV plain proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK

WebDAV HEAD unexistent file with plain proxy
  [Tags]  webdav  head  plainproxy
  ${filename}  Get a unique name
  ${url}  Build webdav secure URL  ${PLAINPROXY_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV plain proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  404 Not Found

WebDAV HEAD existent directory with plain proxy
  [Tags]  webdav  head  plainproxy
  ${url}  Build webdav secure URL  ${PLAINPROXY_SA}  ${TESTDIR}
  ${output}  WebDAV plain proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK

WebDAV HEAD unexistent directory with plain proxy
  [Tags]  webdav  head  plainproxy
  ${url}  Build webdav secure URL  ${PLAINPROXY_SA}  ${TESTDIR}/fakedirectory
  ${output}  WebDAV plain proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  404 Not Found

WebDAV HEAD resource with missing parent with plain proxy
  [Tags]  webdav  head  plainproxy
  ${url}  Build webdav secure URL  ${PLAINPROXY_SA}  ${TESTDIR}/fakedirectory/fakefile.txt
  ${output}  WebDAV plain proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  404 Not Found

WebDAV HEAD busy file with plain proxy
  [Tags]  webdav  head  plainproxy
  ${filename}  Get a unique name
  ${surl}  Build surl  ${PLAINPROXY_SA}  ${TESTDIR}/${filename}
  Init certificate and plain proxy  test0
  ${output}  ${token}  Perform ptp using clientSRM  ${surl}  -p
  Should Contain  ${output}  SRM_SPACE_AVAILABLE
  ${url}  Build webdav secure URL  ${PLAINPROXY_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV plain proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK
  ${output}  Perform abort file using clientSRM  ${surl}  ${token}
  Should Contain  ${output}  SRM_SUCCESS
  Clear all credentials

WebDAV HEAD existent file with plain proxy on an anonymous readable storage area
  [Tags]  webdav  head  plainproxy
  ${filename}  Upload file as anonymous
  ${url}  Build webdav secure URL  ${ANONYMOUS_SA}  ${TESTDIR}/${filename}
  ${output}  WebDAV plain proxy HEAD  ${url}
  Log  ${output}
  Should Contain  ${output}  200 OK
